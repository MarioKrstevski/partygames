# Postgres connection string (Neon in production, any Postgres locally)
DATABASE_URL=postgres://postgres:postgres@localhost:5437/partygames

# better-auth — generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=
# Base URL of the app
BETTER_AUTH_URL=http://localhost:3000

# Admin dashboard — email of the site owner
ADMIN_EMAIL=
